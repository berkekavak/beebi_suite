# During local development the API runs separately (uvicorn on :8000) while
# Next.js serves the UI on :3000. Point the UI at the API:
NEXT_PUBLIC_API_BASE=http://localhost:8000

# In production this is left empty — the static build is served by FastAPI from
# the same origin, so /api/... is same-origin.
