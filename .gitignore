__pycache__/
*.pyc
.databricks/

# Python virtual environments (local only, never deployed)
.venv/
venv/
env/

# Node / frontend build tooling (never deployed)
frontend/node_modules/
frontend/.next/
frontend/.env.local
